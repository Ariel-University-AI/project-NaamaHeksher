# ===========================
# SurveyGIS — Future Phase Dependencies (NOT installed)
# ===========================
# DO NOT pip install this file — these packages are for future phases only.
# Active dependencies are in requirements.txt (Phase 1).
#
# Install a specific phase when ready:
#   pip install psycopg2-binary SQLAlchemy GeoAlchemy2 alembic flask-sqlalchemy flask-cors
#
# Last updated: 2026-05-26
#

# ── Phase 2: PostgreSQL / PostGIS backend ─────────────────────────
psycopg2-binary>=2.9
SQLAlchemy>=2.0
GeoAlchemy2>=0.14
alembic>=1.13
flask-sqlalchemy>=3.1
flask-cors>=4.0

# ── Phase 3: Advanced spatial / export ────────────────────────────
Shapely>=2.0
Fiona>=1.9
geopandas>=0.14
geojson>=3.0
reportlab>=4.0

# ── Phase 4: Dev / CI tooling ─────────────────────────────────────
watchdog>=4.0
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
black>=24.0
flake8>=7.0
